{
  "name": "cotizadorDIT",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm start --prefix cotizador-dit",
    "build": "npm run build --prefix cotizador-dit",
    "test": "npm test --prefix cotizador-dit"
  }
}
